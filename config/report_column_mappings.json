{
    "bank_export_report_2020": {
        "columns": ["transaction_id", "date", "amount", "currency_code", "account_number"],
        "required_columns": ["transaction_id", "date", "amount"],
        "numeric_columns": ["amount"],
        "unique_columns": ["transaction_id"],
        "date_columns": {
            "date": "%Y-%m-%d"
        }
    },
    "bank_export_report_2021": {
        "columns": ["transaction_id", "date", "amount", "currency_code", "account_number", "branch_code"],
        "required_columns": ["transaction_id", "date", "amount"],
        "numeric_columns": ["amount"],
        "unique_columns": ["transaction_id"],
        "date_columns": {
            "date": "%Y-%m-%d"
        }
    },
    "customer_report_2022": {
        "columns": ["customer_id", "first_name", "last_name", "email", "date_of_birth"],
        "required_columns": ["customer_id", "email"],
        "numeric_columns": ["customer_id"],
        "unique_columns": ["email"],
        "date_columns": {
            "date_of_birth": "%Y-%m-%d"
        }
    }
}
